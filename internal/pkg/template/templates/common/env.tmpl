# Database Configuration
DB_HOST=localhost
DB_PORT={{ if eq .DBDriver "PostgreSQL" }}5432{{ else if eq .DBDriver "MySQL" }}3306{{ else }}27017{{ end }}
DB_USER=user
DB_PASSWORD=password
DB_NAME={{ .ProjectName | lower }}

# Server Configuration
SERVER_PORT=8080
APP_ENV=development
